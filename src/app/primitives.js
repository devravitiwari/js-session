/**
 * Write a function that takes color in hex code '#FFFFFF'
 * and return equivalent color in rgb as a string - 'rgb(255, 255, 255)'
 * @param colorInHexCode
 * @returns {string}
 */
function toRGB(colorInHexCode) {
    return '';
}


/**
 * Write a function that takes color in rgb as a string - 'rgb(255, 255, 255)'
 * and return equivalent color in hex code '#FFFFFF'
 * @param colorInHexCode
 * @returns {string}
 */
function toHex(colorInRGBCode) {
    return '';
}